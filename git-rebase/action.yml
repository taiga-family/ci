name: Rebase action
description: Rebase action

runs:
  using: composite
  steps:
    - name: Try rebase locally for check affected
      uses: imba-tjd/rebase-upstream-action@559f4270ba3c290f6009fc7114398a4ea07fa799 # 0.12
      with:
        upstream: ${{ github.repository }}
        branch: ${{ github.base_ref }}
        push: false

    - name: Abort rebase when merge conflict
      if: failure()
      shell: bash
      run: |
        git status
        git rebase --abort
        git status
